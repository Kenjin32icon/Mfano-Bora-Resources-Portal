# Block all direct HTTP access to this folder.
# config.php holds database credentials and the admin API key —
# it must only ever be loaded via PHP require(), never requested directly.
Require all denied
